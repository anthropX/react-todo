import React, { Component } from "react";

export default class About extends Component {
  render() {
    return (
      <React.Fragment>
        <p id="about-desc">Todo app build with create-react-app, react-router, axios, JSON placeholder! :)</p>
      </React.Fragment>
    );
  }
}
